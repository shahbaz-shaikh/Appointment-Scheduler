import { SlotAppointment } from './slot-appointment.model';

describe('SlotAppointment.Model', () => {
  it('should create an instance', () => {
    expect(new SlotAppointment()).toBeTruthy();
  });
});
